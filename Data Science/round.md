기본적으로는 소수점 몇 번째 자리까지 출력할지를 정할 때 사용
컬럼에 '소수점 제외'라는 조건을 걸 때
cond = df['age'] == round(df['age'], 0) 이런 식으로도 쓸 수 있음
(이거를 (df['age'] - int(df['age']) == 0) 이렇게 구현하려고 하면 df['age']가 series라서  int를 못 씌우기 때문에 에러 발생)