
df['new'] = 0
df['할인가'] = df['가격'] * 0.8  # 새로운 column 추가하기
df.head(2)
![[Pasted image 20260819150916.png]]
원래 메뉴, 가격, 칼로리만 있었는데 2개의 컬럼이 옆에 더 생김

df.drop(1, axis=0, inplace=True) # inplace=True면 삭제 결과값이 저장됨
df.drop('new', axis=1, inplace=True) # 컬럼, 행 삭제하기 - axis=0이면 행, 1이면 열 삭제

inplace=True 대신 df_1 = df.drop(1, axis=0) 이런 식으로 할 수도 있는데, 이때 df는 그대로 유지된다.
